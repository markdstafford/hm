//! Deterministic indexable document builders.
//!
//! Issue #10 only materializes pending rows; embedding is added in issue #12.
//! `project_jira_issue` already builds the document rows via the repository
//! helpers, so this module currently has no public API. Leave as a placeholder.
